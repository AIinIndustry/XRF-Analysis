\section{Regression}

\begin{frame}{Regression Task}
    The regression task focuses on \textbf{quantifying} the elemental composition by predicting concentration values from spectral data.

    \vspace{0.3cm}
    \begin{block}{Problem formulation}
        \begin{itemize}
            \item \textbf{Input:} XRF spectrum — 600 energy bins (0–30 keV)
            \item \textbf{Output:} 41 elemental concentrations, non-negative and summing to 1
            \item \textbf{Evaluation:} Masked MAE and R\textsuperscript{2} computed only on \textit{active} elements (ground truth $> 0$)
        \end{itemize}
    \end{block}

    \vspace{0.3cm}
    \begin{block}{Key challenge}
        The output lies on the probability simplex and has \textbf{sparse structure} (only 1–5 of 41 elements active per sample) — standard regression metrics are deceptive, only performance on active elements counts.
    \end{block}
\end{frame}

\begin{frame}{Baseline Models \& Initial Results}
    \begin{columns}[t]
        \begin{column}{0.48\textwidth}
            \begin{block}{Models compared}
                \begin{itemize}
                    \item \textbf{CNNRegressor:} Conv1D + Global Average Pool + dense head
                    \item \textbf{ResNetRegressor:} residual blocks
                    \item \textbf{PLS:} Partial Least Squares (20 components)
                    \item \textbf{Ridge:} L2-regularised, one model per element
                \end{itemize}
            \end{block}
        \end{column}
        \begin{column}{0.48\textwidth}
            \begin{block}{Initial results (5k samples)}
                \small
                \begin{tabular}{lcc}
                    \toprule
                    Model & MAE $\downarrow$ & R\textsuperscript{2} $\uparrow$ \\
                    \midrule
                    CNN    & 0.259 & $-$1.17 \\
                    ResNet & 0.256 & $-$1.12 \\
                    PLS    & 0.227 & $-$0.74 \\
                    Ridge  & 0.210 & $-$0.47 \\
                    \bottomrule
                \end{tabular}
            \end{block}
            \vspace{0.2cm}
            \small All R\textsuperscript{2} values are \textbf{negative} — models worse than mean predictor.
        \end{column}
    \end{columns}
\end{frame}

\begin{frame}{Why CNN Failed: Architecture Issues}
    \begin{columns}[t]
        \begin{column}{0.48\textwidth}
            \textbf{Problems with CNNRegressor V1}
            \begin{itemize}
                \item \textbf{Global Average Pool} collapses all spatial info — cannot distinguish \textit{where} peaks are
                \item \textbf{MaxPool} discards amplitude — concentration $\propto$ peak area, not maximum
                \item \textbf{ReLU + normalise} output collapses to uniform predictions when uncertain
            \end{itemize}
        \end{column}
        \begin{column}{0.48\textwidth}
            \textbf{CNNRegressorV2 fixes}
            \begin{itemize}
                \item \textbf{AvgPool} instead of MaxPool — preserves amplitude
                \item \textbf{No GAP} — retain 15 spatial positions before flatten
                \item \textbf{Softmax} output — stable normalisation
            \end{itemize}
            \vspace{0.3cm}
            \begin{block}{V2 result (10k samples)}
                MAE $= 0.212$, R\textsuperscript{2} $= -0.477$
            \end{block}
            \small Still negative — CNN cannot learn peak positions from data alone at this scale.
        \end{column}
    \end{columns}
\end{frame}

\begin{frame}{Physics-Informed Peak Features}
    \textbf{Key insight:} XRF emission line energies are \textit{physical constants} — we know exactly which spectral bin each element's K-$\alpha$, K-$\beta$, L-$\alpha$ peak appears in.

    \vspace{0.15cm}
    \begin{block}{Multi-line peak feature extraction}
        For each of the 41 elements, integrate counts in $\pm$5-bin windows around all in-range emission lines $\Rightarrow$ \textbf{129 non-redundant features} encoding direct physical knowledge.
    \end{block}

    \vspace{0.15cm}
    \begin{columns}
        \begin{column}{0.48\textwidth}
            \textbf{Also computed:}
            \begin{itemize}
                \item \textbf{Ratio features:} $r_i = p_i / \sum_j p_j$ — directly proportional to concentration
                \item \textbf{Classifier probs:} 41 sigmoid outputs from CNNClassifier — tells which elements are present
            \end{itemize}
        \end{column}
        \begin{column}{0.48\textwidth}
            \begin{block}{Results (10k samples)}
                \footnotesize
                \begin{tabular}{lcc}
                    \toprule
                    Features & MAE & R\textsuperscript{2} \\
                    \midrule
                    Peak integrals (129)  & 0.209 & $-$0.41 \\
                    + ratios              & 0.206 & $-$0.34 \\
                    + clf probs (299)     & 0.206 & $-$0.34 \\
                    \bottomrule
                \end{tabular}
            \end{block}
        \end{column}
    \end{columns}
\end{frame}

\begin{frame}{Per-Element Decomposition}
    \textbf{Key insight:} instead of one model for all 41 outputs, train one model \textit{per element} — each sees only the peaks relevant to it and its overlapping neighbours.

    \vspace{0.15cm}
    \begin{block}{Overlap-aware feature set per element}
        For element $i$: collect peak integrals of all elements whose emission lines fall within $\pm$10 bins of any of $i$'s lines. Fe sees Co, Ni, Cr, O, Na, etc. — its known physical overlaps.
    \end{block}

    \vspace{0.15cm}
    \begin{columns}
        \begin{column}{0.48\textwidth}
            \begin{itemize}
                \item 41 Ridge regressors, each on element-specific features
                \item Classifier probabilities concatenated to every sub-model
                \item Outputs clipped $\geq 0$ and renormalised
            \end{itemize}
        \end{column}
        \begin{column}{0.48\textwidth}
            \begin{block}{Result (10k samples)}
                \begin{center}
                    MAE $= 0.195$, R\textsuperscript{2} $= -0.183$
                \end{center}
                \small Classifier features provide a \textbf{massive jump}: Ridge alone gives $-0.44$.
            \end{block}
        \end{column}
    \end{columns}
\end{frame}

\begin{frame}{Element Transformer}
    \textbf{Key insight:} each element is a \textbf{token} — self-attention lets elements learn inter-element dependencies (overlap, sum-to-1, co-occurrence).

    \vspace{0.1cm}
    \begin{block}{Token embedding (per element)}
        \begin{itemize}
            \item Peak integrals (abs + ratio $r_i = p_i/\sum_j p_j$) for own lines
            \item Normalised atomic number $Z/92$, primary bin index
            \item Classifier probability for that element
        \end{itemize}
    \end{block}

    \vspace{0.1cm}
    \begin{columns}
        \begin{column}{0.48\textwidth}
            \textbf{Architecture:}
            \begin{itemize}
                \item 41 tokens $\rightarrow$ d\_model=128 projection
                \item 3 Transformer encoder layers, 8 heads
                \item Per-element output head $\rightarrow$ Softplus normalisation
            \end{itemize}
        \end{column}
        \begin{column}{0.48\textwidth}
            \begin{block}{Result (10k samples)}
                \begin{center}
                    MAE $= 0.191$, R\textsuperscript{2} $= -0.135$
                \end{center}
                \small Self-attention captures which elements co-occur and how their signals interact.
            \end{block}
        \end{column}
    \end{columns}
\end{frame}

\begin{frame}{Best Model: Transformer V2 Architecture}
\begin{center}
\begin{tikzpicture}[
    box/.style={draw, rounded corners, fill=blue!10, minimum width=1.8cm, minimum height=0.55cm, font=\tiny, align=center},
    attn/.style={draw, rounded corners, fill=orange!20, minimum width=1.8cm, minimum height=0.55cm, font=\tiny, align=center},
    outbox/.style={draw, rounded corners, fill=green!15, minimum width=1.8cm, minimum height=0.55cm, font=\tiny, align=center},
    arr/.style={-{Stealth[length=3pt]}, thick},
    scale=0.82, every node/.style={transform shape}
]

% Left column: spectrum path
\node[box] (spec)    at (0,0)    {Raw spectrum\\600 bins};
\node[box] (cnn)     at (0,-1.1) {CNN encoder\\$600 \to 75 \times d$};
\node[box] (spfeat)  at (0,-2.2) {Spatial features\\$(75, d_\text{model})$};

\draw[arr] (spec)   -- (cnn);
\draw[arr] (cnn)    -- (spfeat);

% Right column: element token path
\node[box] (peaks)  at (3.5,0)   {Peak integrals\\ratios, $Z$, bin, clf};
\node[box] (proj)   at (3.5,-1.1){Input projection\\$\to d_\text{model}$};
\node[attn](selfa)  at (3.5,-2.2){Self-attention\\41 element tokens};

\draw[arr] (peaks)  -- (proj);
\draw[arr] (proj)   -- (selfa);

% Cross-attention bridge
\node[attn](crossa) at (1.75,-3.4){Cross-attention\\tokens $\leftarrow$ spectrum};

\draw[arr] (spfeat) -- (crossa);
\draw[arr] (selfa)  -- (crossa);

% Output
\node[outbox] (head)   at (1.75,-4.5){Per-element heads\\Softplus + normalise};
\node[outbox] (out)    at (1.75,-5.4){$\hat{c} \in \Delta^{40}$\\41 concentrations};

\draw[arr] (crossa) -- (head);
\draw[arr] (head)   -- (out);

\end{tikzpicture}
\end{center}
\vspace{-0.1cm}
\begin{center}
    \small \textbf{MAE $= 0.190$,\; R\textsuperscript{2} $= -0.114$} \quad (10k samples, mixup + curriculum + hard mask)
\end{center}
\end{frame}

\begin{frame}{Training Curve \& Per-Element Accuracy}
    \begin{columns}
        \begin{column}{0.50\textwidth}
            \begin{figure}
                \centering
                \includegraphics[width=\linewidth]{media/regression_training_curve.png}
                \caption{Training vs validation MAE loss.}
            \end{figure}
        \end{column}
        \begin{column}{0.50\textwidth}
            \begin{figure}
                \centering
                \includegraphics[width=\linewidth]{media/regression_per_element_mae.png}
                \caption{Per-element MAE on test set.}
            \end{figure}
        \end{column}
    \end{columns}
\end{frame}

\begin{frame}{Results Summary \& Model Progression}
    \begin{center}
    \small
    \begin{tabular}{lcc}
        \toprule
        \textbf{Model} & \textbf{MAE} $\downarrow$ & \textbf{R\textsuperscript{2}} $\uparrow$ \\
        \midrule
        CNN V1 (Global Avg Pool)           & 0.259 & $-$1.17 \\
        CNN V2 (AvgPool + spatial head)    & 0.212 & $-$0.477 \\
        Ridge (full spectrum)              & 0.210 & $-$0.472 \\
        MLP on 299 features (ratio + clf)  & 0.206 & $-$0.343 \\
        Per-element Ridge + clf probs      & 0.195 & $-$0.183 \\
        Element Transformer                & 0.191 & $-$0.135 \\
        \textbf{Transformer V2 (cross-attn)} & \textbf{0.190} & $\mathbf{-0.114}$ \\
        \bottomrule
    \end{tabular}
    \end{center}
    \vspace{0.1cm}
    \small R\textsuperscript{2} $> 0$ requires $\sim$12\% further MSE reduction — achievable with $\geq$20k samples.
\end{frame}

\begin{frame}{Physical Bottleneck: Detector Transmission}
    \begin{columns}
        \begin{column}{0.62\textwidth}
            \begin{figure}
                \centering
                \includegraphics[width=\linewidth]{media/regression_detector_transmission.png}
            \end{figure}
        \end{column}
        \begin{column}{0.36\textwidth}
            \begin{block}{X-ray attenuation law}
                \[
                    T = \prod_i e^{-\frac{\mu}{\rho}\big|_i \rho_i d_i}
                \]
                \vspace{-0.2cm}
                {\small $\mu/\rho$: mass att. coeff. (NIST, cm\textsuperscript{2}/g), $\rho$: density, $d$: thickness}
            \end{block}
            \vspace{0.15cm}
            \small
            \textbf{O K-$\alpha$, Be 0.127\,mm:}\\
            $e^{-8273 \times 1.85 \times 0.013} \approx e^{-194} \approx 0$\\[3pt]
            \textbf{O K-$\alpha$, PP 0.5\,$\mu$m + He:}\\
            $e^{-9961 \times 0.86 \times 5{\times}10^{-5}} \approx 0.65$
        \end{column}
    \end{columns}
\end{frame}
